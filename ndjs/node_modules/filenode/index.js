var fs = require("fs");

fs.readFile("result.txt", "utf-8", function (err, data) {
    if (err) {
        console.log(err);
    }
    else {
        console.log(data);
    }
});
try {
    var data = fs.readFileSync("result.txt", "utf-8");
}
catch (e) {

}
// fs.writeFile("result.txt", "hi", function (err) {
//     if (err) {
//         console.log(err);
//     }
// });
fs.stat('result.txt', function (err, stat) {
    if (err) {
        console.log(err);
    } else {
        // 是否是文件:
        console.log('isFile: ' + stat.isFile());
        // 是否是目录:
        console.log('isDirectory: ' + stat.isDirectory());
        if (stat.isFile()) {
            // 文件大小:
            console.log('size: ' + stat.size);
            // 创建时间, Date对象:
            console.log('birth time: ' + stat.birthtime);
            // 修改时间, Date对象:
            console.log('modified time: ' + stat.mtime);
        }
    }
});
var stat = fs.statSync("result.txt");
console.log("stat:"+stat.isFile());
var rs = fs.createReadStream('result.txt');
var ws = fs.createWriteStream('output1.txt');

rs.pipe(ws, { end: false });
global.setTimeout((err,data)=>{ws.write("还没完事")},3000);